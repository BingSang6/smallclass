# -*- coding: utf-8 -*-
"""gen_guwen.py — 语文·小古文题库生成器
题源：primary-tutor-skill/knowledge-bases/chinese-primary-curriculum.md 第三部分（小古文 11 篇，公版）
题型：接下句 / 接上句 / 字词释义（curated）/ 认出处
产出：data/banks/guwen.json（按年级 3~6，篇内按题序分 6 段位）
"""
import json, os, random, re
random.seed(20260821)

# (篇名, 年级, 原文, 释义 [(字词, 正确释义, 干扰1, 干扰2)])
TEXTS = [
    ('司马光', 3, '群儿戏于庭，一儿登瓮，足跌没水中。众皆弃去，光持石击瓮破之，水迸，儿得活。',
     [('庭', '庭院', '厅堂', '院子外'), ('瓮', '口小肚大的陶缸', '水井', '木桶'),
      ('皆', '全、都', '一些人', '大部分'), ('迸', '涌出', '流干', '消失')]),
    ('守株待兔', 3, '宋人有耕者。田中有株。兔走触株，折颈而死。因释其耒而守株，冀复得兔。兔不可复得，而身为宋国笑。',
     [('株', '树桩', '树枝', '庄稼'), ('走', '跑', '走路', '离开'),
      ('释', '放下', '拿起', '丢失'), ('冀', '希望', '害怕', '等待')]),
    ('精卫填海', 4, '炎帝之少女，名曰女娃。女娃游于东海，溺而不返，故为精卫，常衔西山之木石，以堙于东海。',
     [('少女', '小女儿', '少女', '第一个女儿'), ('溺', '淹没、溺水', '游泳', '口渴'),
      ('堙', '填塞', '挖开', '包围'), ('故', '因此、所以', '故意', '缘故')]),
    ('王戎不取道旁李', 4, '王戎七岁，尝与诸小儿游。看道边李树多子折枝，诸儿竞走取之，唯戎不动。人问之，答曰：树在道边而多子，此必苦李。取之，信然。',
     [('尝', '曾经', '尝试', '品尝'), ('竞走', '争着跑过去', '比赛跑步', '慢慢走'),
      ('唯', '只有', '唯一', '以为'), ('信然', '确实这样', '不太信', '果然错了')]),
    ('囊萤夜读', 4, '胤恭勤不倦，博学多通。家贫不常得油，夏月则练囊盛数十萤火以照书，以夜继日焉。',
     [('恭勤', '谦逊勤勉', '恭敬老师', '勤劳致富'), ('练囊', '白色薄绢做的口袋', '练习的口袋', '布书包'),
      ('以夜继日', '夜晚接着白天（日夜不停）', '从早到晚玩耍', '白天接着白天'), ('通', '通晓、明白', '通过', '普通')]),
    ('铁杵成针', 4, '磨针溪，在象耳山下。世传李太白读书山中，未成，弃去。过是溪，逢老媪方磨铁杵。问之，曰：欲作针。太白感其意，还卒业。',
     [('未成', '没有完成学业', '没有成功', '没有长成'), ('方', '正在', '方法', '方向'),
      ('感其意', '被她的意志感动', '感谢她的心意', '感觉有意思'), ('卒业', '完成学业', '毕业了', '军队作业')]),
    ('自相矛盾', 5, '楚人有鬻盾与矛者，誉之曰：吾盾之坚，物莫能陷也。又誉其矛曰：吾矛之利，于物无不陷也。或曰：以子之矛陷子之盾，何如？其人弗能应也。',
     [('鬻', '卖', '买', '制造'), ('誉', '夸耀', '名誉', '诬陷'),
      ('陷', '刺穿', '陷害', '掉进'), ('弗能应', '不能回答', '大声回应', '不想回答')]),
    ('杨氏之子', 5, '梁国杨氏子九岁，甚聪惠。孔君平诣其父，父不在，乃呼儿出。为设果，果有杨梅。孔指以示儿曰：此是君家果。儿应声答曰：未闻孔雀是夫子家禽。',
     [('诣', '拜访', '遇见', '邀请'), ('乃', '就、于是', '竟然', '但是'),
      ('示', '给……看', '表示', '指示'), ('应声', '随着声音（立刻回答）', '应该出声', '声音很大')]),
    ('学弈', 6, '弈秋，通国之善弈者也。使弈秋诲二人弈，其一人专心致志，惟弈秋之为听；一人虽听之，一心以为有鸿鹄将至，思援弓缴而射之。虽与之俱学，弗若之矣。',
     [('通国', '全国', '通过国家', '许多国'), ('诲', '教导', '会议', '后悔'),
      ('鸿鹄', '天鹅', '大雁', '老鹰'), ('弗若之矣', '不如他啊', '比他强啊', '追上他了')]),
    ('两小儿辩日', 6, '孔子东游，见两小儿辩斗，问其故。一儿曰：我以日始出时去人近，而日中时远也。一儿曰：我以日初出远，而日中时近也。',
     [('辩斗', '辩论、争论', '打架', '斗嘴吵架'), ('以', '认为', '因为', '所以'),
      ('去', '距离', '离开', '去掉'), ('日中', '正午', '一天中间', '太阳中间')]),
    ('伯牙鼓琴', 6, '伯牙鼓琴，锺子期听之。方鼓琴而志在太山，锺子期曰：善哉乎鼓琴，巍巍乎若太山。少选之间而志在流水，锺子期又曰：善哉乎鼓琴，汤汤乎若流水。',
     [('鼓', '弹奏', '鼓掌', '敲鼓'), ('志', '心志、想到', '志气', '记住'),
      ('善哉', '好啊', '善良啊', '擅长啊'), ('少选', '一会儿、不久', '少年选择', '很少挑选')]),
]


def sentences(text):
    """按标点切成句子（保留句中标点）"""
    parts = re.split(r'(?<=[。？！])', text)
    return [p.strip() for p in parts if p.strip()]


out = []
all_titles = [t[0] for t in TEXTS]
# 跨篇句子池（干扰项不足时借用其他篇的句子）
ALL_SENTS = [s for _, _, text, _ in TEXTS for s in sentences(text) if 4 <= len(s) <= 28]

for title, grade, text, notes in TEXTS:
    qs = []
    ss = [s for s in sentences(text) if 4 <= len(s) <= 28]
    # 接下句 / 接上句（相邻句对）
    for i in range(len(ss) - 1):
        a, b = ss[i], ss[i + 1]
        pool = [x for x in ss if x != b and abs(len(x) - len(b)) <= 8]
        if len(pool) < 2:
            pool = [x for x in ALL_SENTS if x != b and abs(len(x) - len(b)) <= 8]
        if len(pool) >= 2:
            dz = random.sample(pool, 2)
            qs.append({
                'q': '《' + title + '》接下句：' + a + '（　）',
                'a': b, 'options': dz,
                'wrongReasons': ['《' + title + '》原句：' + a + b, '先读懂句意，再顺着故事往下背'],
                'tag': '小古文·' + title
            })
        pool2 = [x for x in ss if x != a and abs(len(x) - len(a)) <= 8]
        if len(pool2) < 2:
            pool2 = [x for x in ALL_SENTS if x != a and abs(len(x) - len(a)) <= 8]
        if len(pool2) >= 2:
            dz2 = random.sample(pool2, 2)
            qs.append({
                'q': '《' + title + '》接上句：（　）' + b,
                'a': a, 'options': dz2,
                'wrongReasons': ['《' + title + '》原句：' + a + b, '倒着背一遍，前后句就都牢了'],
                'tag': '小古文·' + title
            })
    # 字词释义
    for w, right, w1, w2 in notes:
        qs.append({
            'q': '《' + title + '》中「' + w + '」的意思是？',
            'a': right, 'options': [w1, w2],
            'wrongReasons': ['「' + w + '」= ' + right, '小古文字词要联系整句话来推'],
            'tag': '小古文·' + title
        })
    # 认出处（用释义正确项造句）
    if notes:
        w, right, _, _ = notes[0]
        others = [t for t in all_titles if t != title]
        qs.append({
            'q': '小古文里讲到「' + w + '」（' + right + '），出自哪一篇？',
            'a': '《' + title + '》', 'options': ['《' + x + '》' for x in random.sample(others, 2)],
            'wrongReasons': ['出自《' + title + '》', '把 11 篇小古文的故事各用一句话概括，就好记了'],
            'tag': '小古文·' + title
        })
    # 篇内按题序分 6 段位
    n = len(qs)
    for i, q in enumerate(qs):
        q['grade'] = grade
        q['level'] = min(6, 1 + i * 6 // n)
        q['speak'] = q['q'].replace('（　）', '什么').replace('（）', '什么')
        q['id'] = 'w%d-%02d' % (grade, len(out))
        out.append(q)

path = os.path.join(os.path.dirname(__file__), '..', 'data', 'banks', 'guwen.json')
with open(path, 'w', encoding='utf-8') as f:
    json.dump(out, f, ensure_ascii=False, indent=1)
print('生成', len(out), '道小古文题 →', os.path.normpath(path))
for g in (3, 4, 5, 6):
    print('  %d年级: %d 题' % (g, sum(1 for x in out if x['grade'] == g)))
